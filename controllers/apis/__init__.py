"""REST API controller interfaces.

HTTP routes for the ``apis/product/...`` path live under the ``apis`` package
(see ``apis/product``), not here—this package only holds controller abstractions.
"""
